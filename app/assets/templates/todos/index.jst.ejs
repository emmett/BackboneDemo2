<h2>Index</h2>

<ul>
  <% todos.each(function (todo) { %>
    <!--
        don't use `get` to insert un-escaped user generated content
      -->
    <li><%= todo.get("title") %></li>
  <% }); %>
</ul>
